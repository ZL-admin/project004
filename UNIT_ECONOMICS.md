# 单位经济测算报告 —— 二次元 AI 角色扮演 App

> 路线：纯 SFW + 双商店上架 + 现成 LLM API（不自训模型）
> 定价基准：2026-05 各家公开报价。重跑：`python3 unit_economics.py`
> ⚠️ 所有结论依赖下方"假设"。上线后务必用**真实日志**校准 token 结构、使用强度、转化率，再重跑。

## TL;DR（先看这三条）

1. **模型选型直接决定生死。** 同样的业务假设下，用 `gemini-2.5-flash-lite` 毛利 **+69%**，用 `gemini-3.5-flash` 毛利 **-429%**（每 1000 用户每月净亏 1500 美元）。差别 20 倍以上。
2. **成本几乎全在 input。** 每条消息 input ≈ 4340 token、output ≈ 220 token（约 20:1）。因为每条消息都要重发"角色卡+记忆+历史"。所以两个降本杠杆是：**①用便宜模型 ②提高缓存命中率**。
3. **分层用模型是必须的。** 免费层用最便宜的（Flash-Lite 档），付费/重度用户才上更贵的模型。一刀切用高价模型，用户越多亏越多。

## 关键结论数字（每 1000 MAU / 月）

| 模型 | 每条$ (缓存) | 毛利率 | 打平转化率(+广告) | 评价 |
|---|---|---|---|---|
| **gemini-2.5-flash-lite** | $0.00033 | **+69%** | 已被广告覆盖 | ✅ 免费层首选 |
| gpt-4o-mini | $0.00062 | +41% | 0.60% | ✅ 可选备份 |
| deepseek-v3 | $0.00068 | +35% | 0.93% | ✅ 备份/分流 |
| gemini-3.1-flash-lite | $0.00093 | +12% | 2.26% | ⚠️ 偏紧 |
| gemini-2.5-flash | $0.00127 | -20% | 4.37% | ⚠️ 仅付费层 |
| gemini-3.5-flash | $0.00556 | -429% | 不可行 | ❌ 全局不可用 |

> 注：基准转化率假设为 3%。可以看到，用 Flash-Lite 时**免费用户的广告收入($0.15/人/月)就已覆盖其 LLM 成本($0.098)**，意味着免费用户不亏钱、付费用户纯赚——这是这门生意能扩张的前提。

## 三个最敏感的杠杆（按影响排序）

1. **缓存命中率**：把 `history_tokens` 的稳定前缀 + `system_prompt` + `memory` 做成可缓存前缀。命中率从 0% → 50% 可降本约 25–40%。工程上回报最高。
2. **history_tokens（上下文窗口）**：3000 token 是稳态平均。如果做"无限记忆"把整段历史塞进去，成本会线性爆炸——必须靠**摘要 + 向量检索**控制窗口大小，而不是无脑拼接。
3. **付费用户使用强度**：付费用户在 60% 毛利下，Flash-Lite 可支持每月约 8500 条消息——很宽裕。但若给付费用户上 `gemini-2.5-flash`，预算骤降到 2189 条，重度用户就会变成亏损户，需要软上限/降速。

## 推荐配置（MVP 阶段）

| 用途 | 模型 | 理由 |
|---|---|---|
| 免费层主力 | `gemini-2.5-flash-lite` | 最便宜、SFW 友好、自带安全过滤 |
| 付费层升级 | `gemini-2.5-flash` 或 `gpt-4o-mini` | 质量更好，控制在付费档 |
| 备份/容灾分流 | `deepseek-v3` | 防单一供应商故障/限流 |
| 内容审核 | OpenAI Moderation API | **免费**，不占额度，做上架的安全兜底 |

## 假设清单（请逐项用真实数据替换）

| 参数 | 当前假设 | 备注 |
|---|---|---|
| system_prompt_tokens | 800 | 角色卡+系统指令 |
| memory_tokens | 500 | 长期记忆/摘要 |
| history_tokens | 3000 | 滑动窗口（稳态平均）|
| ai_reply_tokens | 220 | roleplay 回复偏长 |
| cache_hit_ratio | 50% | 工程优化目标 |
| free / paid 月消息 | 300 / 1500 | 上线后按真实分布校准 |
| paid_conversion | 3% | 陪伴类常见 2–5% |
| sub_price | $9.90 | 商店抽 30%，净 $6.93 |
| ad_arpu_free | $0.15 | 免费用户月广告收入 |

## 这份测算没有覆盖（别忘了还有这些成本）

- **获客成本 (CAC)**：通常是真正的大头。本表只算了 LLM 边际成本（COGS），没算买量。CAC vs LTV 要单独建模。
- 服务器/数据库/带宽、语音(TTS)与生图（Phase 2 再说）、人工审核、退款与白嫖、第三方 SDK 抽成。
- 商店首年 30% 抽成，满一年部分订阅降到 15%（本表按 30% 保守计）。

---

**结论**：这门生意单位经济**能跑通**，前提是——免费层死守 Flash-Lite 档、做好前缀缓存、用摘要控制上下文长度、并把转化率做到 ~2% 以上。真正的风险不在 LLM 成本，而在 **获客成本** 和 **过审/合规**。
