# PRD —— 二次元 AI 角色扮演 App（MVP）

> 路线：纯 SFW · 双商店上架（iOS + Android）· 现成 LLM API · 二次元垂直切入
> 配套文档：`UNIT_ECONOMICS.md`（成本模型）、`TECH_DESIGN.md`（技术方案）、`ROADMAP.md`（任务拆解）
> 状态：v0.1 草案 —— 待评审

## 1. 目标与非目标

### 1.1 一句话定位
"和你最爱的二次元角色聊天"——一个面向动漫/二次元爱好者、内容安全(SFW)、可上架双商店的 AI 角色扮演 App。

### 1.2 为什么是二次元垂直（而非综合）
- 避开与 Character.AI 的正面同质化竞争（综合赛道它免费、海量、品牌强）。
- 二次元用户：付费意愿强、社区聚集（Discord/Reddit/TikTok/B站/微博）、ASO 关键词明确、UGC 创作活跃。
- 题材边界清晰，便于做"角色质感"和运营话题。

### 1.3 MVP 目标（可衡量）
| 指标 | 目标 | 说明 |
|---|---|---|
| 核心循环可用 | 发现→聊天→记忆→自建角色 全打通 | 功能完整性 |
| 过审 | iOS + Android 双商店通过（17+ 分级）| 上架硬门槛 |
| 留存 | D1 ≥ 35% / D7 ≥ 15% | 验证"聊得上头" |
| 参与 | 人均会话消息数 ≥ 20/天 | 粘性信号 |
| 单位经济 | 免费用户 LLM 成本被广告覆盖 | 见 UNIT_ECONOMICS.md |

### 1.4 非目标（MVP 不做，避免范围膨胀）
- ❌ 语音通话 / TTS（Phase 2）
- ❌ AI 生图 / 头像生成（Phase 2）
- ❌ 自训模型 / 微调（用现成 API）
- ❌ 成人/NSFW 内容（路线选择即排除，且与上架冲突）
- ❌ 社交关系链 / 角色广场点赞评论（Phase 2）
- ❌ Web 版（先 App，Phase 2 补 PWA）

## 2. 目标用户与场景

- **核心用户**：13–25 岁动漫爱好者（注意：内容 SFW，但分级建议 17+，见 §6）。
- **典型场景**：
  1. 刷到喜欢的角色 → 开聊 → 沉浸式 roleplay。
  2. 找不到想要的角色 → 自己捏一个（设定人设/开场白）。
  3. 每天回来和"固定角色"继续昨天的剧情（记忆是关键体验）。

## 3. 功能需求（MVP 范围）

### P0（必做，缺一不可）
1. **账号体系**：邮箱/第三方登录（Apple 登录在 iOS 必须有）、登出、**账号删除**（过审强制）。
2. **角色发现页**：预置 30–50 个二次元角色，按题材/标签浏览，搜索。
3. **沉浸式聊天**：SSE 流式逐字输出；显示角色头像/名字；保留会话。
4. **会话记忆**：滑动窗口 + 长期记忆摘要（用户回来能接上）。
5. **自建角色（UGC）**：名字、头像（先用预置图库，不做生图）、人设、开场白、标签、公开/私有。
6. **内容安全**：输入+输出双层审核拦截（过审与合规的生命线，见 TECH_DESIGN §5）。
7. **基础合规**：隐私政策、用户协议、年龄确认门、举报/拉黑入口。

### P1（MVP 后紧接着做）
8. **货币/订阅**：免费层（限速+广告）+ 订阅（RevenueCat）。
9. **记忆增强**：向量检索长期记忆。
10. **多模型分层**：免费/付费走不同模型（成本控制）。

### P2（后续）
语音、生图、角色广场社交、Web/PWA、活动运营位。

## 4. 核心用户流程

```
首次启动 → 年龄确认门(17+) → 登录(Apple/Google/邮箱)
   → [发现页] 浏览/搜索角色
        → 点入角色 → [聊天页] 流式对话（带记忆）
        → 顶部入口：举报/拉黑/重置会话
   → [创建] 自建角色 → 填写人设/开场白 → 保存(公开/私有) → 直接开聊
   → [我的] 会话列表 / 我的角色 / 设置(隐私政策/账号删除/登出)
```

## 5. 关键体验原则（决定留存）
1. **首条回复要快**：SSE 流式，首 token < 1s 体感。
2. **角色不能崩**：系统提示词把人设/语气/边界写死；长对话靠记忆防"失忆"。
3. **SFW 但不扫兴**：审核要拦真正违规内容，但不能动不动就"我不能回答"——这是 SFW 产品体验的核心难点，需要精心调教提示词与审核阈值。
4. **冷启动不空**：预置角色覆盖几个热门题材，新用户进来就有的聊。

## 6. 合规与分级（上架硬门槛，不可跳过）

> 详细清单见 `ROADMAP.md` 的"过审/合规"章节。这里列原则。

- **分级**：尽管 SFW，AI 开放对话类建议自评 **17+ / Teen-to-Mature**，并**如实填写**（虚标分级是最常见下架原因）。
- **内容审核**：输入与输出都过审核 API；命中即拦截并兜底回复；保留审计日志。
- **未成年人保护**：年龄确认门；不主动诱导情感依赖；提供求助资源位（涉及自伤等话题时）。
- **隐私**：隐私政策 + 数据删除路径（App 内可删账号与数据）；聊天数据加密存储；明确告知数据用途。
- **Apple 特定**：必须支持"通过 Apple 登录"（若有第三方登录）；UGC 内容需有举报+拉黑+24h 处理承诺（Apple Guideline 1.2）。
- **第三方**：LLM 供应商的数据使用条款需符合隐私政策声明。

## 7. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 过审被拒（内容/分级/UGC 审核）| 上不了架 | Phase 1.5 先把审核+举报+账号删除做齐；分级如实填 |
| 获客成本 > LTV | 烧钱不增长 | 先小预算软启动；靠社区/ASO/短视频低成本获客 |
| LLM 成本失控 | 越多越亏 | 分层模型 + 前缀缓存 + 摘要控上下文（见 UNIT_ECONOMICS）|
| 同质化、留不住人 | 留存低 | 死磕"角色质感"+记忆；二次元题材运营 |
| 版权角色（知名 IP）| 法务风险 | MVP 用原创/泛化设定为主，知名 IP 谨慎；提供权利人投诉通道 |
| 单一供应商故障/限流 | 服务中断 | 多供应商容灾分流 |

## 8. 里程碑（详见 ROADMAP.md）
- **Phase 1（MVP，3–4 周）**：P0 全部功能跑通。
- **Phase 1.5（1–2 周）**：过审/合规收口。
- **Phase 2（变现，2–3 周）**：订阅 + 记忆增强 + 分层模型。
- **Phase 3**：软启动 → 获客 → 数据迭代。
